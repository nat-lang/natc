
let nulSet = {};
let numSet = {1, 2, 3};
let strSet = {"a", "b", "c"};

// membership.

assert("a" in strSet and "b" in strSet);
assert(1 in numSet);

// as characteristic function.

assert(numSet(1));

// nested.

let set = {{1,2,3}, {3,4,5}};

assert({1,2,3} in set);
assert({3,4,5} in set);

assert(!({1} in set));
assert(!({3} in set));