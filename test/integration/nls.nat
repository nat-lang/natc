import src/core/json

let main = (path, source) => {
  let module = compile(path, source);
  
  module.__import__();

  let moduleAst = ast(module.module);
  
  print jsonEncode({"success": true, "data": moduleAst});
};

main("foo", "let z = [1 2 3];");