

// native length.

let seq = [1,2,3].values;

assert(len(seq) == 3);

// length protocol.

class Lengthy {
  init(a) => { this.length = a; }
  __len__() => { return this.length; }
}

assert(len(Lengthy(0)) == 0);