
import test/integration/composition as composition
import src/core/latex

composition.__import__();

let moduleAst <- composition.function;

let doc = TexDocument(
  "article",
  ["tikz, tikz-qtree"],
  [node[1].resolve() | node in moduleAst.children,
                       node is ASTGlobalValueAssignment,
                       node[1].resolve() is Tree]
);

print doc.render();
