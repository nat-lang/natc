
global iter = x => {
  let idx = 0
  let end = len(x)
  
  let more = () => idx < end
  let next = () => {
    idx = idx + 1
    return x[idx - 1]
  }

  return {
    "more": more,
    "next": next
  }
}
