
class Tree {
  init() => {
    this.data = nil;
    this.children = [];
  }

  setData(data) => {
    this.data = data;
    return this;
  }

  addChild(child) => {
    this.children.add(child);
    return this;
  }
}