class Type {}

class Metaclass < Type {
  isSubclass(klass) => {
    while (klass) {
      // invoke pointer equality so that `==` can bottom out.
      if (klass is this) return true;

      klass = klass.superclass;
    }
    return false;
  }

  __eq__(klass) => this.isSubclass(klass) or klass.isSubclass(this)
}
