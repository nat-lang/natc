
class Map < __map__ {
  get(key) => this[key]
  set(key, val) => {
    this[key] = val;
    return this;
  }
  
  __len__() => len(this.entries())
  __curr__(idx) => this.entries()[1]

  keys() => fmap(this, fst)
  values() => fmap(this, snd)
}