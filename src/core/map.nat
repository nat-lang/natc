
class Map < __map__ {
  get(key) => this[key]
  set(key, val) => {
    this[key] = val;
    return this;
  }

  __len__() => len(this.entries())
  __curr__(idx) => this.entries()[1]

  keys() => [entry[0] | entry in this.entries()]
  values() => [entry[1] | entry in this.entries()]
}
