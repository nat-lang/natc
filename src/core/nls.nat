
let main = (path, source) => {
  let module = compile(path, source);
  module.__import__();
  let trees = [val | val in module.values(), val is AnnotatedTree];
  let document = TreeDocument(trees);

  print jsonEncode({
    "success": true,
    "tex": document.render()
  });
};