
// The vm relies on a handful of objects to do its job.
// Instantiate them here. Note that this module must be
// executed after the rest of the core modules.

import core/type/system
import core/composition

// We keep the quantifiers in their own namespace
// so that grammars can use the identifiers.

import core/quantification as Quantification