
let private = () => [
  "__class__"
];

class Object extends Base {
  keys() => {
    let f = x => x in private();
    let g = super.keys();
    return g.filter(f);
  }
  values() => [this[key] | key in this.keys()];
  entries() => [(key, this[key]) | key in this.keys()];

  __len__() => len(this.keys());
  __iter__() => iter(this.entries());

  extend(obj: Object) => {
    for (x in obj)
      this[x[0]] = x[1];
  }

  fvs() => Set();
}