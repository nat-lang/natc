let f <- () => true && false;

assert(f is ASTClosure);
assert(f.children == [
  ASTConditional(
    ASTLiteral(true),
    ASTBlock(
      f,
      ASTReturn(
        ASTLiteral(false)
      )
    )
  ),
  ASTReturn(
    ASTLiteral(true)
  ),
  ASTImplicitReturn(
    ASTLiteral(nil)
  )
]);