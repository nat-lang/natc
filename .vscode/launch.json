{
  "configurations": [
    {
      "name": "Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/nat",
      "args": [
        "${workspaceFolder}/test/integration/index",
      ],
    }
  ],
}